(defwidget bg_widgets []
    (box :orientation "v" :space-evenly true
        (box :orientation "h" :space-evenly false :valign "center" :halign "bottom"
            (big_clock)
        )
        (box :orientation "h" :space-evenly false :valign "center"
            (weather)
        )
    )
)
